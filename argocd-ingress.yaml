apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-ingress
  namespace: argocd
  # Аннотации УДАЛЕНЫ, они нам больше не нужны!
spec:
  rules:
  - host: "argocd.100.126.89.59.nip.io"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 80    # <--- ВАЖНО: ПОРТ 80
