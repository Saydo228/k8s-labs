apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-ai-chart.fullname" . }}
  labels:
    {{- include "my-ai-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-ai-chart.selectorLabels" . | nindent 4 }}
